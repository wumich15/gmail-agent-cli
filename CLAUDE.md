# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty scaffolding: it contains only `README.md` ("gmail app") and `LICENSE` (MIT). There is no source code, package manifest, build system, linter, or test suite yet, and it is not currently a git repository.

Because there is no existing architecture or tooling to document, there are no build/lint/test commands to list. When code is added to this project, update this file with:
- The actual build, lint, and test commands (and how to run a single test) once a package manager/framework is chosen
- The high-level architecture once there are multiple files/modules whose interaction isn't obvious from a directory listing
