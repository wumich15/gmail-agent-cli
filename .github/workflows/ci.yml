# Continuous integration: everything that can run without a production secret.
#
# The production plan requires formatting/lint, type checking, the full unit
# suite, a build, dependency review, secret scanning, and vulnerability
# scanning on every supported platform before a release can be considered.
name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
      # --frozen-lockfile: a release must build the dependency set that was
      # reviewed, never one resolved fresh at build time.
      - run: pnpm install --frozen-lockfile
      # The gateway is a separate deployable with its own dependency set. Its
      # sources are linted with its own tsconfig, and the unit suite exercises
      # the checks it performs before any Firebase or provider call, so both
      # need its node_modules present.
      - run: pnpm --dir gateway install --no-frozen-lockfile --ignore-workspace
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm gateway:typecheck
      - run: pnpm test
      - run: pnpm build

  supply-chain:
    name: Supply chain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level high
      - name: License inventory
        run: pnpm licenses list --prod || true
      - name: Software bill of materials
        run: pnpm sbom
      - uses: actions/upload-artifact@v5
        with:
          name: sbom
          path: sbom.cdx.json
      - name: Secret scan
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v5
      - uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: high
